{{input_file}} を階層的な箇条書きのある markdown に変換して、拡張子を.mdに変更したファイル名で保存してください。

以下のルールに従って変換してください：
- タイトルと各セクションに適切な見出しレベル（#, ##, ###, ####）を設定
- 項目を箇条書き（-）や番号付きリストで整理
- 階層構造をインデントで表現
- 表形式が適切な場合は表（|）を使用
- 重要なポイントは太字（**）で強調
- コード関連の用語は適切にバッククォート（`）で囲む

入力ファイル: {{input_file}}
出力ファイル: {{input_file}}の拡張子を.mdに変更したもの
